# 三層防護部署說明

對應這次討論：「下次程式修改導致學生無法登入或儲存，能涵蓋此問題嗎」。
這份說明涵蓋三層的安裝步驟，照順序做完即可。三層互相獨立，也可以先只裝一層，之後再補其他兩層。

## 檔案異動清單

| 檔案 | 狀態 | 說明 |
|---|---|---|
| `.github/workflows/canary.yml` | 新增 | Layer 2：部署後自動健康檢查 |
| `test-suite/run-tests.js` | **修改** | 讓 `--student` 模式不再強制要求老師的 `session.json`，CI 才能跑 |
| `test-suite/notify-discord.js` | 新增 | Layer 2：測試失敗時推送 Discord |
| `test-suite/check-rule-diff.js` | 新增 | Layer 3：rule.txt 高風險區塊變更偵測 |
| `test-suite/rules-tests/` 整個資料夾 | 新增 | Layer 1：Firestore Rules 模擬器單元測試 |
| `Step5_CheckRuleDiff.bat` | 新增 | Layer 3 的 Windows 一鍵執行捷徑 |

`run-tests.js` 的修改只有一處：原本不管有沒有要跑老師端測試，一開始就會檢查
`session.json` 存不存在，沒有就直接中止。改成「只有真的要跑老師端測試時才檢查」，
這樣 `node run-tests.js --student` 才能在沒有人手動登入老師帳號的 CI 環境裡執行。
**這個修改不影響你現在手動執行 `Step2_RunTests.bat`（完整跑老師+學生）的行為**，
那個情境下 `session.json` 還是照樣會被要求要存在。

## Layer 1：Firestore Rules 模擬器單元測試

獨立資料夾，跟其他兩層沒有依賴關係，最不急但最值得先裝，因為裝好之後不需要再管它，
以後每次改 rule.txt 前自己跑一下就有信心。

詳細步驟見 `test-suite/rules-tests/README.md`，懶人版：

```
雙擊 test-suite\rules-tests\RunRulesTest.bat
```

第一次執行會自動檢查 Java、自動 `npm install`，第一次跑測試還會自動下載 Firestore
模擬器本體（需要網路）。我已經在我這邊的沙盒環境驗證過整個流程能跑到「下載模擬器」
這一步，只差實際下載這一步因為我的環境網路限制無法驗證，但程式邏輯（45 個測試案例、
複製 rule.txt、啟動 emulator、規則判斷邏輯）都已經用手寫的「正確版 vs 錯誤版」rule.txt
測過，邏輯正確。你的電腦只要能連網應該可以直接跑完。

## Layer 2：部署後健康檢查（GitHub Actions + Discord）

需要你做兩件事：

### 1. 建立 Discord Webhook

到你想收通知的 Discord 頻道 → 頻道設定 → 整合 → Webhook → 新增 Webhook → 複製網址。

### 2. 在 GitHub 設定 4 個 Secrets

到你的 GitHub repo → Settings → Secrets and variables → Actions → New repository secret，
依序新增：

| Secret 名稱 | 值 |
|---|---|
| `FIREBASE_API_KEY` | 跟 `test-suite/.env` 裡的 `FIREBASE_API_KEY` 一樣 |
| `TEST_STUDENT_EMAIL` | 跟 `test-suite/.env` 裡的 `TEST_STUDENT_EMAIL` 一樣 |
| `TEST_STUDENT_PASSWORD` | 跟 `test-suite/.env` 裡的 `TEST_STUDENT_PASSWORD` 一樣 |
| `DISCORD_WEBHOOK_URL` | 剛剛複製的 Discord webhook 網址 |

設定完成後，這個 workflow 會在三個時機自動跑：

- 推送到 `main` 分支後幾分鐘內（如果你的預設分支不是 `main`，要去改
  `.github/workflows/canary.yml` 裡 `branches: [main]` 那一行）
- 每天台北時間早上 8 點（即使你那天沒有改任何東西，也會檢查一次，
  防範 Firebase 或 GitHub Pages 那端自己出問題）
- 你想手動跑的時候：GitHub repo → Actions → 「學生端健康檢查（Canary）」→ Run workflow

跑的內容就是現有的 `node run-tests.js --student`，直接打你的正式環境
（`https://ka-dot-dot.github.io/internship-journal/student.html`），跟你平常手動跑
`Step2_RunTests.bat` 測的是同一套東西、同一個正式網站，只是自動化、定期、而且失敗會通知你。

失敗時 Discord 會收到類似這樣的訊息：

```
🚨 產學班實習月記系統｜學生端自動化健康檢查失敗
結果：43/45 通過，2 失敗
失敗項目：
  ✗ S-WRITE-REAL Firestore CREATE 規則驗證（學生身份 REST 直接寫入）
      journals CREATE 被拒（403）：CREATE rule 可能仍用 !keys().hasAny()...
完整報告與截圖：https://github.com/.../actions/runs/...
```

點進連結可以下載完整的 `test-report.txt` 跟失敗截圖（GitHub Actions 的 Artifacts，保留 14 天）。

### 目前範圍只到學生端，老師端還沒自動化

老師端測試需要 `session.json`（老師用 Google 帳號互動登入產生），CI 沒辦法自動做這件事。
要補的話，作法跟學生端一樣：另外申請一個測試老師帳號、在 Firebase Console 開
Email/Password 登入、把這個帳號加進 `/admins/`，就能仿照 `auto-student-session.js`
寫一個 `auto-teacher-session.js`。這次先不做，等你需要再說。

## Layer 3：rule.txt 高風險區塊變更偵測

不需要額外設定，裝好馬上就能用。原理：每次比對你「上次 commit 的 rule.txt」跟
「現在工作區的 rule.txt」，把 `students`、`journals`、`studentBindings` 這幾個高風險
match block 整段抽出來顯示新舊差異，同時掃描是否出現「用 `!hasAny()` 檢查 teacher
欄位不存在」這種已知會出問題的寫法（不管是不是這次新改的，出現就示警）。

用法（在專案根目錄）：

```
雙擊 Step5_CheckRuleDiff.bat
```

或部署前手動執行：

```
node test-suite\check-rule-diff.js
```

沒有高風險變更會顯示「可以安全繼續部署」並正常結束；有高風險變更會把新舊內容整段印出來，
並提示你去跑 Layer 1 的模擬器測試確認過後，再決定要不要繼續。這個工具只是提醒，
不會真的擋住你部署，最終決定權還是在你。

我已經用「正確版」對「故意改成 `!hasAny()` 錯誤寫法」的 rule.txt 實際測過，
包括區塊差異顯示、危險寫法掃描、`--confirm` 跳過機制、以及不指定檔案時自動抓
git 版本比對，四種情境都驗證過，行為符合預期。

## 三層怎麼分工

- **平時改 rule.txt**：先跑 Layer 3（10 秒內知道改了高風險區塊沒有），
  有警告就跑 Layer 1（幾分鐘內知道規則邏輯對不對），確認沒問題再部署。
- **部署之後**：Layer 2 自動在背景跑，部署完幾分鐘內、之後每天，
  確認「正式環境 + 目前程式碼」整體真的能讓學生登入、寫月記，壞了 Discord 會通知你。

這樣規則邏輯本身（Layer 1）、人為改動時的注意力（Layer 3）、部署後的即時發現（Layer 2）
三個角度都有人在看，不會再像這次一樣，要等學生回報才知道存不進去。
